I am currently using Ollama (see @../config/llm_config.py). I wish to change to llama.cpp (I installed it in my environment
  already via pip). Create a task to accomplish this with tests/test_llama_cpp.py. This should require two tasks.
