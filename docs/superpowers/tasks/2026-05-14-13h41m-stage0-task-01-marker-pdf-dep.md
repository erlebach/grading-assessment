# Stage 0 Task 1 — Add `marker-pdf` dependency

**Plan:** `docs/superpowers/plans/2026-05-14-stage0-translate-sources.md` §Task 1 — **Status:** Complete — **Date:** 2026-05-14 13:41

> Part of the **Stage 0** plan (15 tasks). Distinct from the 23-task foundation plan recorded as `2026-05-13-task-NN-*.md`.

## Commits

| SHA | Message |
|---|---|
| `1fffe42` | `build(grading-plugin): add marker-pdf dependency for Stage 0 PDF translation` |

## Files

- Modified: `pyproject.toml` (+1 dependency line)
- Modified: `uv.lock` (regenerated by `uv add`)

## What

- `uv add marker-pdf` → `marker-pdf>=1.5.5` added to `[project] dependencies`; `uv.lock` re-solved.
- Provides the `marker_single` CLI on PATH for Stage 0 PDF translation. Stage 0 invokes it via subprocess (never `import`s it), so CI stubs the CLI.

## Tests

- `marker_single --help` exits 0.
- Full plugin suite: 103 passing.

## Reviewers

- Spec compliance: ✅ — `marker-pdf` in `[project] dependencies` (not a dev group); only `pyproject.toml` + `uv.lock` changed; commit message exact.
- Code quality: ✅ Approved — `uv.lock` machine-generated, declaration style consistent. Minor informational notes: `uv` re-solve dropped transitive `rsa` and bumped `google-auth` (harmless); `marker-pdf` pulls in ~17 transitive packages.

## Notes

- `uv add` required disabling the sandbox (uv cache at `~/.cache/uv` is outside the sandbox-writable set).
