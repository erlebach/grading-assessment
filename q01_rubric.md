Question: 
 "1. In the context of a data table, precisely distinguish an \u201C\
  object\u201D from an \u201Cattribute,\u201D and give two alternative names for each\
  \ (as used in data mining practice)."
----------------------------------------------------------------------
Rubric:

1. Object definition and role (4 points): Clearly defines an object as the observational unit/record (a single data case represented by one row) and states that it is the entity being described or analyzed.
2. Attribute definition and role (4 points): Clearly defines an attribute as a variable/feature (a property measured or recorded for each object, represented by a column) and states that it provides the descriptors used to characterize objects.
3. Standard alternative names (2 points): Provides two commonly used alternative names for “object” and two commonly used alternative names for “attribute,” each set matching established data-mining terminology.

Total: 10 points
----------------------------------------------------------------------
Rubric:

1. Object definition and role (3 points): Clearly defines an object as the entity/instance represented by a row (or record) in the table, i.e., the unit of analysis being described by measured/recorded values.
2. Attribute definition and role (3 points): Clearly defines an attribute as a variable/feature represented by a column, i.e., a named property/dimension used to describe objects and taking values across rows.
3. Alternative names for object (2 points): Provides two correct alternative names used in data mining practice for “object” (e.g., instance, sample, record, observation, data point, case, tuple).
4. Alternative names for attribute (2 points): Provides two correct alternative names used in data mining practice for “attribute” (e.g., feature, variable, field, dimension, predictor, input).
   Total: 10 points
----------------------------------------------------------------------
Rubric:

1. Structural mapping in a data table (3 points): States that objects correspond to rows/records (each row is one entity being analyzed) and attributes correspond to columns/fields (each column is one measured property).
2. Conceptual distinction (3 points): Precisely distinguishes that an object is the unit of analysis being described, whereas an attribute is a descriptive property/variable whose values characterize objects.
3. Two alternative names for object (2 points): Provides two correct alternative names for “object” as used in data mining (e.g., instance, case, observation, sample, record, tuple).
4. Two alternative names for attribute (2 points): Provides two correct alternative names for “attribute” as used in data mining (e.g., feature, variable, field, dimension, predictor).
   Total: 10 points
----------------------------------------------------------------------
Rubric:

1. Object as observational unit (4 points): Defines an object as a single observational unit (entity/case) represented by one row (tuple/record) whose components are its recorded values across attributes.
2. Attribute as descriptive variable (4 points): Defines an attribute as a named descriptive variable represented by one column that specifies a property/dimension measured or recorded for each object.
3. Accepted synonyms (2 points): Supplies two standard alternative names for “object” and two standard alternative names for “attribute,” using terminology common in data mining practice.
   Total: 10 points
----------------------------------------------------------------------
----------------------------------------------------------------------
----------------------------------------------------------------------
----------------------------------------------------------------------
